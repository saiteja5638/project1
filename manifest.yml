---
applications:
  - name: project1
    path: dist
    memory: 256M
    instances: 1
    buildpacks:
      - html5-apps-repo
    services:
      - html5-apps-repo-rt  # Binding to HTML5 apps repository service

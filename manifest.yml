---
applications:
  - name: project1
    path: dist
    memory: 256M
    instances: 1
    disk_quota: 512M  # Optional but recommended
    buildpacks:
      - nodejs_buildpack  # Optionally add a version or leave as is
    env:
      NODE_ENV: production  # Optional, based on your environment